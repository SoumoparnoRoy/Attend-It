# Keep the daemon heap within what a typical 8-16 GB machine can spare. Asking
# for more than the free memory makes the daemon JVM die on startup, which the
# launcher reports as the misleading "Unable to establish loopback connection".
org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
